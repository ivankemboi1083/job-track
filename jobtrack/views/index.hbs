
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Job-track | Available Vacancies</title>
    <link rel="stylesheet" href="/css/jobs.css" />
    <link rel="icon" href="/favicon.png" type="image/png" />
  </head>
  <body>
    <header>
      <div class="container">
        <h1 class="logo">Job-track</h1>
        <p class="tagline">Track your career. Apply with confidence.</p>
        <nav class="homepage-nav">
          {{#if guest}}
            <a href="/users/login">Login</a>
            <a href="/users/signup">Sign Up</a>
          {{else}}
            <a href="/users/dashboard">Dashboard</a>
            <a href="/users/logout">Logout</a>
          {{/if}}
        </nav>
      </div>
    </header>

    <main class="container">
      {{#if guest}}
        <section class="landing-center">
          <h2 class="jobs-title">Welcome to Job Track</h2>
          <p class="landing-desc">Sign up or log in to view the latest remote jobs and track your applications!</p>
          <div class="landing-actions">
            <a href="/users/signup" class="landing-btn">Sign Up</a>
            <a href="/users/login" class="landing-btn secondary">Login</a>
          </div>
        </section>
      {{else}}
        <section class="search-filter">
          <form class="job-search-form" method="GET" action="/">
            <input type="text" name="search" class="search-box" placeholder="🔍 Search job titles..." value="{{search}}" />
            <select class="filter-dropdown" name="category">
              <option value="">Filter by Category</option>
              <option value="tech">Technology</option>
              <option value="design">Design</option>
              <option value="marketing">Marketing</option>
            </select>
            <button type="submit" class="job-search-btn">Search</button>
          </form>
        </section>

        <section class="job-listings centered-listings">
          <h2>Available Vacancies</h2>
          {{#if error}}
            <div class="error-message">{{error}}</div>
          {{/if}}
          {{#if jobs.length}}
            <div class="job-cards-wrapper">
              {{#each jobs}}
                <div class="job-card compact square-card">
                  <div class="job-main-info">
                    <span class="icon-title" title="Job Title" style="vertical-align:middle;">
                      <svg width="18" height="18" fill="#1976d2" viewBox="0 0 24 24"><path d="M4 7V6a4 4 0 0 1 8 0v1h4a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2h4zm2-1a2 2 0 1 1 4 0v1H6V6z"/></svg>
                    </span>
                    <h3 style="display:inline; font-size:1.1rem; margin-left:6px;">{{title}}</h3>
                  </div>
                  <div class="job-meta-row">
                    <span class="icon-location" title="Location" style="vertical-align:middle;">
                      <svg width="16" height="16" fill="#388e3c" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5A2.5 2.5 0 1 1 12 6a2.5 2.5 0 0 1 0 5.5z"/></svg>
                    </span>
                    <span style="margin-right:12px;">{{candidate_required_location}}</span>
                    <span class="icon-type" title="Job Type" style="vertical-align:middle;">
                      <svg width="16" height="16" fill="#fbc02d" viewBox="0 0 24 24"><path d="M17 10.5V7a5 5 0 0 0-10 0v3.5A2.5 2.5 0 0 0 3 13v5a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-5a2.5 2.5 0 0 0-4-2.5zM7 7a3 3 0 0 1 6 0v3H7V7zm12 11a.997.997 0 0 1-1 1H6a.997.997 0 0 1-1-1v-5a.5.5 0 0 1 .5-.5h13a.5.5 0 0 1 .5.5v5z"/></svg>
                    </span>
                    <span>{{job_type}}</span>
                  </div>
                  <button class="show-more-btn" type="button" onclick="toggleJobDetails(this)">Show More</button>
                  <div class="job-details" style="display:none;">
                    <div class="job-description">{{{safeHTML description}}}</div>
                    <a href="{{url}}" class="apply-btn" target="_blank">Apply Now</a>
                  </div>
                  <form class="add-app-form" action="/jobs/apply" method="POST" style="margin-top:10px;">
                    <input type="hidden" name="jobTitle" value="{{title}}" />
                    <input type="hidden" name="company" value="{{company_name}}" />
                    <input type="hidden" name="location" value="{{candidate_required_location}}" />
                    <input type="hidden" name="url" value="{{url}}" />
                    <input type="hidden" name="category" value="{{category}}" />
                    <input type="hidden" name="jobType" value="{{job_type}}" />
                    <button type="submit" class="add-app-btn">Add to Applications</button>
                  </form>
                </div>
              {{/each}}
            </div>
          {{else}}
            <p>No jobs found.</p>
          {{/if}}
        </section>
      {{/if}}
    </main>

    <footer>
      <div class="container">
        <p>&copy; 2025 Job-track. All Rights Reserved.</p>
      </div>
    </footer>

    <script>
      function toggleJobDetails(btn) {
        const details = btn.parentElement.querySelector('.job-details');
        if (details.style.display === 'none') {
          details.style.display = 'block';
          btn.textContent = 'Show Less';
        } else {
          details.style.display = 'none';
          btn.textContent = 'Show More';
        }
      }
    </script>
    <script src="/javascripts/jobs.js"></script>
  </body>
</html>
